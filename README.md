# Node Project + Typescript

1. Clone .env.template to .env and configure the environment variables.
2. Run npm install to install the dependencies.
3. If you need a database, configure the docker-compose.yml file and run docker compose up -d to start the desired services.
4. Run npm run dev to start the project in development mode
